# 로컬 LLM 서버 주소
LLM_BASE_URL=http://localhost:8080

# 사용할 모델명 (LLM 서버 시작 시 model 값과 일치해야 함)
LLM_DEFAULT_MODEL=gemma-4-2b

# 요청 타임아웃 (초)
LLM_REQUEST_TIMEOUT=120

# ─── API 서버 설정 ──────────────────────────────────────────────────────────────
APP_HOST=0.0.0.0
APP_PORT=8000
APP_LOG_LEVEL=info
